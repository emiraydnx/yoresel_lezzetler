{
  "name": "yoresel-lezzetler-functions",
  "description": "Firebase Cloud Functions for Yoresel Lezzetler",
  "private": true,
  "main": "index.js",
  "engines": {
    "node": "20"
  },
  "scripts": {
    "lint": "node --check index.js && node --check stats.js && node --check stats.test.js",
    "test": "node --test stats.test.js"
  },
  "dependencies": {
    "firebase-admin": "^13.6.0",
    "firebase-functions": "^6.4.0"
  }
}
